<UserControl x:Class="RemoteServer.Views.ChatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="ChatList" Margin="5" BorderThickness="0" Background="#0f3460" Foreground="White">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,2">
                        <TextBlock Text="{Binding Timestamp}" FontSize="10" Foreground="#888"/>
                        <TextBlock FontWeight="Bold" Foreground="#00d2ff" Text="{Binding Sender}"/>
                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <TextBox x:Name="ChatInput" Height="30" Width="300" Margin="0,0,5,0"/>
            <Button Content="Enviar" Width="70" Click="SendChat_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
