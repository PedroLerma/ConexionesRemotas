<Window x:Class="RemoteServer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RemoteServer.Views"
        Title="Conexión Remota - Servidor" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1a1a2e" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="CÓDIGO DE CONEXIÓN" Foreground="#888" FontSize="12" HorizontalAlignment="Center"/>
                <TextBlock x:Name="CodeText" Text="{Binding ConnectionCode}"
                           Foreground="#00d2ff" FontSize="48" FontWeight="Bold"
                            HorizontalAlignment="Center" FontFamily="Consolas"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#16213e" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="{Binding Status}" Foreground="#eee" FontSize="14" HorizontalAlignment="Center"/>
        </Border>

        <TabControl Grid.Row="2" Margin="0,0,0,10">
            <TabItem Header="Chat">
                <local:ChatControl x:Name="ChatControlView"/>
            </TabItem>
            <TabItem Header="Archivos">
                <Grid Margin="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Transferencia de archivos" Foreground="#888" FontSize="14" Margin="0,0,0,10"/>
                        <Button Content="Enviar archivo..." Padding="15,8" Click="SendFile_Click"/>
                        <ProgressBar x:Name="FileProgress" Height="20" Margin="0,10,0,0" Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
            <Button x:Name="StartBtn" Content="Iniciar Sesión" Padding="20,10"
                    IsEnabled="{Binding CanStart}" Click="StartBtn_Click" Margin="0,0,10,0"/>
            <Button x:Name="StopBtn" Content="Detener" Padding="20,10"
                    IsEnabled="{Binding IsRunning}" Click="StopBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
